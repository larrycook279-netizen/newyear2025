# Лендинг «Письмо от настоящего Деда Мороза»

Премиальный, эмоциональный лендинг, который переносит родителей и детей в атмосферу новогоднего чуда. Страница создана по ТЗ: акцент на ручной работе, тепле свечей и истории, которую рассказывает сайт.

## 📁 Структура проекта

```
├── index.html          # Сборка страницы (подключает dist/css и dist/js)
├── dist/               # Готовые для публикации ассеты
│   ├── css/main.css
│   └── js/main.js
├── src/
│   ├── scss/           # Исходные стили, разбитые по base/components/sections
│   └── js/main.js      # Исходный скрипт (идентичен dist/js/main.js)
├── scripts/preview.mjs # Локальный сервер предпросмотра (node)
└── package.json
```

## ✨ Основные особенности

- Герой-блок с «снегом» и деталями ручной работы.
- Секции «О письме», «Особенности», эмоциональные цитаты и процесс из пяти шагов.
- Таблица форматов и стоимости, карточки преимуществ, отзывы родителей.
- Таймер обратного отсчёта до 15 декабря и форма заявки с валидацией.
- Лёгкие анимации: снег, мерцание свечи, появление блоков при скролле.
- Адаптивная сетка (mobile first, 320–1440px+), семантическая верстка, БЭМ-классы.
- Метатеги OpenGraph, favicon, шрифты Playfair Display и Inter из Google Fonts.

## 🚀 Быстрый старт

```bash
npm install   # не обязателен — зависимостей нет, но оставлен для совместимости
npm run preview
```

Команда `npm run preview` поднимает простой Node.js-сервер и отдаёт содержимое каталога `dist/` на `http://localhost:4173`.

> При необходимости можно воспользоваться `python -m http.server 4173` и открыть `dist/index.html` вручную.

## 🧰 Работа с исходниками

- Стили пишутся в `src/scss`. При отсутствии компилятора SCSS в окружении можно редактировать `dist/css/main.css` напрямую, однако рекомендуется использовать Dart Sass и настроить автоматическую сборку.
- Скрипт `src/js/main.js` содержит генерацию снега, scroll-reveal, обратный отсчёт и отправку формы.
- Декоративные визуалы создаются градиентами и псевдоэлементами в CSS, поэтому бинарных изображений в репозитории нет.

### Рекомендованная схема сборки SCSS → CSS

Установите [Dart Sass](https://sass-lang.com/install) и добавьте команду в `package.json`:

```json
"scripts": {
  "build:css": "sass src/scss/main.scss dist/css/main.css --no-source-map --style=compressed"
}
```

## 📄 Подключение формы

Форма «Анкета помощников Деда Мороза» ожидает endpoint в атрибуте `data-endpoint`:

```html
<form id="order-form" data-endpoint="https://example.com/api/orders" novalidate>
```

Скрипт отправляет JSON через `fetch`. При отсутствии endpoint пользователю выводится сообщение о том, что заявка сохранена локально. Подключить Telegram-бота можно через промежуточный сервер, который принимает JSON и пересылает сообщение.

## ✅ Чек-лист перед релизом

- [ ] Заменить заглушки изображений на реальные фотографии/рендеры.
- [ ] Сконвертировать изображения в WebP и обновить ссылки.
- [ ] Скомпилировать SCSS → CSS (при необходимости минифицировать).
- [ ] Подключить реальный endpoint формы и протестировать отправку.
- [ ] Проверить отображение в Chrome, Safari, Firefox, Edge.
- [ ] Пройтись по чек-листу доступности (контраст, `alt`-тексты, табуляция).

## 📦 Лицензия

Проект распространяется по лицензии [MIT](./LICENSE) — используйте и адаптируйте под свои задачи.
